# 🚀 FastAdmin — Web Admin Panel for Fast_IO

**FastAdmin** — это «админка следующего поколения» для файловых баз данных Fast_IO.

- **Zero dependencies** — только чистый PHP 8.1+, Vanilla JS и современный CSS  
- **Полная поддержка всех функций** Fast_IO (text + binary)  
- **Современный cyber-tech дизайн** с тёмной/светлой темой и сканлайн-эффектом  
- **Мгновенная работа** даже с таблицами в десятки гигабайт  

![FastAdmin Screenshot](https://raw.githubusercontent.com/commeta/fast_io/refs/heads/main/test/FastAdmin/fastadmin-screenshot.png "FastAdmin")

---

## ✨ Что умеет FastAdmin

### Управление таблицами
- Автоматическое сканирование папки `fast_io_data/`
- Визуальное разделение **Text** и **Binary** таблиц (по наличию `.index`)
- Быстрое создание новой таблицы (выбор типа Text/Binary)
- Удаление таблицы одним кликом (с подтверждением)

### Text-таблицы (классические строки key value)
- **Browse** — пагинированный просмотр через `file_get_keys` (все 6 режимов)
- **Search** — поиск по ключу и по любому PCRE2-регулярному выражению (`file_search_line` + `file_search_array`)
- **Insert** — вставка с выравниванием (`file_insert_line`)
- **Update** — одиночное и пакетное обновление (`file_update_line` + `file_update_array`)
- **Erase / Replace** — удаление и замена по ключу
- **Pop** — операции LIFO-стека (`file_pop_line`) с поддержкой offset
- **Callback** — мощный построчный процессор `file_callback_line` (до 10 параметров в callback!)
- **Defrag** — дефрагментация с удалением помеченных строк
- **Replicate** — копирование таблиц
- **Analize** — полная диагностика файла (`file_analize`)

### Binary-таблицы (key → binary blob)
- Просмотр индекса
- `file_push_data` — добавление бинарных данных
- `file_search_data` — поиск и получение блока данных
- `file_defrag_data` — дефрагментация пары data + index
- Специальные операции над индексом (erase по ключу в `.index`)

### Инструменты разработчика
- Встроенный **PCRE2-тестер** (`find_matches_pcre2`) с визуализацией всех совпадений
- Поддержка **всех режимов `mode`** (0…100+) с подсказками в интерфейсе
- Режимы **+100 (Log mode)** — без блокировки файла (идеально для журналов)
- Защита от опасных действий (пустой ключ автоматически блокируется)
- JSON-редакторы для batch-операций (`file_select_array`, `file_update_array`)

---

## 🚀 Как запустить 

1. Убедитесь, что расширение `fast_io` загружено (`extension=fast_io.so`)
2. Скопируйте файл `fastadmin.php` в корень вашего сайта
3. Откройте в браузере: `http://ваш-сайт/fastadmin.php`

Панель автоматически создаст папку `fast_io_data/` и начнёт работу.

---

## Технические особенности

- **PHP 8.1+** (использует современные фичи: `str_ends_with`, named arguments и т.д.)
- **Native JS** — 100% без фреймворков, размер ~45 КБ (минифицировано)
- **Безопасность** — встроенная защита от пустых ключей в деструктивных операциях
- **Производительность** — полностью использует все оптимизации Fast_IO (посекторное чтение/запись, динамический буфер, flock и т.д.)

---

## 📖 Руководство пользователя

### Общий вид интерфейса

FastAdmin — это **одностраничное SPA-приложение** (single-file). Работает через AJAX, без перезагрузок страницы.

**Структура экрана:**

- **Верхняя панель (Topbar)**  
  Логотип FastAdmin • статус подключения расширения • переключатель тёмной/светлой темы • индикатор «живой» системы (зелёная точка с пульсацией).

- **Левая боковая панель (Sidebar, 260 px)**  
  - Форма «Новая таблица»: поле имени + выпадающий список **Text** / **Binary**.  
  - Живой список всех таблиц в папке `fast_io_data/`.  
    Каждая строка: иконка типа, имя файла, бейдж (`text` или `binary`), размер в удобочитаемом формате.  
    Активная таблица подсвечивается акцентным цветом.

- **Основная область**  
  - При первом запуске — приветственный экран с ретро-ASCII-артом и короткой справкой.  
  - После выбора таблицы — полноэкранный таб-интерфейс.

### Типы таблиц

| Тип      | Файлы на диске          | Назначение                              | Что видно в панели                     |
|----------|-------------------------|-----------------------------------------|----------------------------------------|
| **Text** | `table.dat`             | Классические строки `key value\n`       | Все операции со строками + LIFO-stack |
| **Binary** | `table.dat` + `table.dat.index` | `key → binary blob` (произвольные данные) | Просмотр индекса + push/search/defrag |

Панель автоматически определяет тип по наличию `.index`-файла и переключает доступные вкладки.

### Вкладки таблицы

#### 1. Browse (Просмотр)
- Пагинация `start` / `limit` (по умолчанию 25 строк).
- 6 режимов отображения (выпадающий список с подсказками):
  - 0 — полная информация (trim + метаданные)
  - 1 — только строка
  - 2 — только trimmed строка
  - 3 — только счётчик
  - 4 — только ключи
  - 5 — только полные строки
- Для **Binary** автоматически открывается отдельная вкладка **Browse Index** (показывает `offset:size`).

#### 2. Search (Поиск)
- Поле для ключа **или** PCRE2-регулярного выражения.
- Поддержка всех режимов `file_search_array` (включая 10–23 с выделением совпадений).
- Результаты в виде красивой таблицы + счётчик найденного.
- Кнопка «Test PCRE2» открывает встроенный тестер регулярных выражений.

#### 3. Insert (Вставка)
- **Text**: многострочное поле + Align (4096 по умолчанию) + Mode.
- **Binary**: отдельные поля Key + Value (поддерживает любой бинарный контент через textarea).

#### 4. Update (Обновление)
- **Одиночное** — `file_update_line`: новая строка + позиция + align + mode.
- **Пакетное** — `file_update_array`: JSON-редактор вида  
  `[[ "новый контент", offset, size ], ... ]`

#### 5. Operations (Операции)
**Для Text-таблиц:**
- Pop (LIFO-стек) с offset и 4 режимами
- Erase по ключу
- Replace по ключу
- Replicate (копирование таблицы)
- (Defrag закомментирован в UI — доступен через прямой вызов)

**Для Binary-таблиц:**
- Defrag Data
- Replicate (data + index)
- Erase из индекса (отдельная кнопка)

#### 6. Analyze (Анализ)
- Полная статистика `file_analize` (mode 0 и 1).
- Для Binary — отдельный блок статистики индекса.
- Графическое отображение `flow_interruption`, min/max/avg длины, last_symbol и сырой JSON.

#### 7. PCRE2 Tester (глобальный инструмент)
- Отдельная панель (открывается кнопкой в любой вкладке).
- Ввод паттерна и subject → мгновенный результат со всеми совпадениями, смещениями и длинами.
- Использует ту же библиотеку PCRE2 10.42, что и все поисковые функции.

### Дополнительные возможности UI

- **Защита данных**: все операции с пустым ключом (`erase`, `replace`, `search`) автоматически блокируются с понятным сообщением.
- **Уведомления**: зелёные/красные/жёлтые notif-блоки внизу каждой карточки операции.
- **JSON-редакторы**: с автоматическим форматированием для batch-операций.
- **Тема**: сохраняется в `localStorage`, плавный переход, адаптированный scanline-эффект для светлой темы.
- **Мобильная адаптивность**: работает на планшетах и телефонах (grid перестраивается).
- **Клавиатура**: `Esc` закрывает модальные окна, `Enter` в поле имени создаёт таблицу.

### Рекомендации по работе с большими объёмами

- Batch-операции (`select_array`, `update_array`) позволяют работать с тысячами записей за один AJAX-запрос.


---

